# 專案設定：個人品牌網站

> 這份設定是「護欄＋內容」。網站的視覺美學（配色、字體配對、動效、排版細節）交給 frontend-design 發揮，這裡只負責把它框在正確的內容、風格方向與技術限制內，不要在這份檔案裡把顏色字體鎖死。

## 溝通方式

- 一律使用繁體中文回覆，並用白話文解釋你做了什麼。
- 每次修改前先說明打算怎麼改，等我確認再執行。
- 一次只改一個區塊，不要整站重寫。

## 網站名稱與定位

- 網站名稱：Oberon Lai
- 一句話定位：我幫需要網站的企業與創業者，把卡住的 WordPress 網站與流程處理到能正常賺錢。

## 目標受眾

- 對象：潛在客戶（手上有網站或系統問題、想找人解決的企業窗口與創業者）。
- 我能幫他們解決的問題：解決具體的 WordPress／外掛／金流串接問題；把重複的人工流程自動化以提升效率。
- 因此網站文案要優先回答的問題：你能處理我這種問題嗎？怎麼合作？怎麼找到你？

## 網站目標與行動

- 主要目標：讓客戶認識我、建立信任，接到案子或長期合作。
- 主要行動按鈕：「聊聊你的需求」→ mailto:m615926@gmail.com

## 頁面結構（一頁式，依順序）

- 開頭自我介紹
- 關於我
- 服務項目
- 作品案例
- 客戶評價
- 聯絡方式

- **成品至少要有 6 個區塊**，每個區塊都要有實質內容（標題＋幾句文案／幾個項目），不要只放一個標題就帶過，讓頁面看起來完整、有份量。

## 文案規範

- 不要使用的字詞：業界第一、頂尖、革命性、賦能、全方位、無縫、一站式等誇大與空泛形容詞。
- 主標題必須在一行內講出「我幫誰解決什麼」。
- 沒有查證過的客戶名稱、數字、成效一律寫「（待補）」，絕不編造。

## 視覺風格（給方向與細節線索，具體執行交給 frontend-design）

- 風格方向與心情關鍵字：簡約文青、乾淨、克制、留白、雜誌感、有質感。**明確排除科技風**（不要深色霓虹、發光效果、網格科技背景、數字跑動）。
- 排版感覺：大量留白、雜誌感網格、細分隔線、左右不對稱。
- 字體感覺：質感襯線標題 ＋ 乾淨無襯線內文。
- 色彩方向：低彩度米白／灰階為底，加一個沉穩點綴色。
- 插圖／圖像風格：少插圖，以大圖攝影與細緻線條為主；沒有照片時用線條與留白撐起版面，不要塞裝飾特效。
- 動畫與互動：克制優雅，捲動輕柔淡入、文字慢慢浮現，hover 只做細微回饋。

## 技術限制（硬性護欄，一定要遵守）

- **純靜態多檔案網站**：`index.html` + `style.css` + `script.js`，圖片放在專案根目錄。可直接部署 GitHub Pages。
- **絕對不要使用 React／Vue／Next.js 等框架，不要 npm install、不要 build 步驟、不要打包工具。**
- **CSS、JS、圖片一律用相對路徑 `./`**（例如 `./style.css`、`./photo.jpg`）。
- 所有內容寫在單一頁面，用錨點連結切換段落。手機版需正常顯示。

## 部署到 GitHub Pages（做完就上線，並在瀏覽器打開）

- GitHub 帳號：`oberonlai`；主站 repo：`oberonlai.github.io`；網址：`https://oberonlai.github.io/`（沒有子目錄）。
- 首次部署：`git init` → `git add -A` → `git commit` → `gh repo create "$OWNER.github.io" --public --source=. --remote=origin --push` → `gh api --method POST "repos/$OWNER/$OWNER.github.io/pages" -f "source[branch]=main" -f "source[path]=/"`。
- 之後每次改完網站檔案，**自動執行** `git add -A && git commit -m "更新網站" && git push`，不用問要不要 push。Pages 約 30–60 秒後更新。
- 要看成果一律開線上網址，**不要開本機 `index.html`（`file://`）**。

## 不要起本機伺服器、不要自己做預覽檔

- 不要 `python -m http.server`、`php -S`、`npx serve`、`launch.json`，也不要做暫時合併版或臨時檔。

## 速度優先：一次生成就部署

- **一次把網站生成完就部署，不要做截圖打磨迴圈。** 禁止逐段截圖、反覆微調。
- 上線後要不要再美化，等我看完提出，再針對我說的地方改（改完一樣自動 push）。

## 完工前品質檢查

- [ ] `index.html`／`style.css`／`script.js` 與圖片都在，沒有框架、沒有 node_modules、沒有 build 產物。
- [ ] CSS／JS／圖片全部用相對路徑 `./`。
- [ ] hero 主標題一行講清楚「我幫誰解決什麼」。
- [ ] 內容用的是 website-brief.md 的真實文字，沒有 lorem ipsum 或假資料。
- [ ] 手機版不爆版，圖片都有 alt。

## 素材位置（做網站前一定要先讀專案資料夾）

- 動工前先掃過專案資料夾：履歷／自我介紹檔、個人照片與作品圖（用 `./` 相對路徑引用並補 alt）、`website-brief.md`。
- 找不到的資訊寧可標「（待補）」或詢問，不要自己編造經歷、數字或客戶案例。
