# 專案設定：個人品牌網站

> 這份設定是「護欄＋內容」。網站的視覺美學（配色、字體配對、動效、排版細節）交給 frontend-design plugin 發揮，這裡只負責把它框在正確的內容、風格方向與技術限制內，不要在這份檔案裡把顏色字體鎖死。

## 溝通方式

- 一律使用繁體中文回覆，並用白話文解釋你做了什麼。
- 每次修改前先說明打算怎麼改，等我確認再執行。
- 一次只改一個區塊，不要整站重寫。

## 網站名稱與定位

- 網站名稱：陳大文
- 一句話定位：我幫團隊把重複的人工流程做成系統，從前端到部署一手包辦。

## 目標受眾

- 對象：招募方與合作方（技術主管、人資、新創創辦人）。
- 我能幫他們解決的問題：提升團隊作業效率；解決具體的全端開發問題（React／Node.js 開發、Docker 容器化、AWS 部署）。
- 因此網站文案要優先回答的問題：這個人做過什麼規模的東西？技術深度到哪裡？可以馬上接手哪一段工作？

## 網站目標與行動

- 主要目標：求職用，讓履歷之外的實力被看到。
- 主要行動按鈕：「寄信談談機會」→ `mailto:chendawen@example.com`；次要行動為 GitHub 連結。

## 頁面結構（一頁式，依順序）

- 開頭自我介紹（姓名、一句話定位、形象照、主要行動）
- 關於我（履歷摘要改寫成人話，加上現況與可談的合作方式）
- 我能解決的問題（三項服務，各有標題與說明）
- 技能與工具（依履歷的技能熟練度呈現）
- 經歷與學歷（工作經驗、學歷、證照，依時間排序）
- 作品案例（履歷工作經驗中的三個實際專案與成效數字）
- 推薦人（王大偉，推薦語待補）
- 聯絡方式（Email、GitHub、所在城市）

- **成品至少要有 6 個區塊**，每個區塊都要有實質內容（標題＋幾句文案／幾個項目），不要只放一個標題就帶過。

## 文案規範

- 不要使用的字詞：頂尖、卓越、無縫、賦能、一站式、業界領先等誇大空泛的形容詞。
- 主標題必須在一行內講出「我幫誰解決什麼」。
- 數字一律引用履歷上的真實數字（30% 作業效率、日均 10,000 次瀏覽、5 萬次下載、15% 滿意度），不要新增沒有出處的數字。

## 視覺風格（給方向與細節線索，具體執行交給 frontend-design）

- 風格方向與心情關鍵字：簡約文青、克制、有質感、像編排講究的紙本作品集。
- 排版感覺：大量留白、雜誌感網格、細分隔線、左右不對稱。
- 字體感覺：質感襯線標題＋乾淨無襯線內文，技術名詞用等寬字當標記。
- 色彩方向：低彩度米白／灰階為底，點綴色取自履歷上的赭紅。
- 插圖／圖像風格：少插圖，以大圖人像攝影與細緻線條刻度為主。
- 動畫與互動：克制，捲動輕柔淡入、文字慢慢浮現，不要視差與發光。

## 技術限制（硬性護欄，一定要遵守）

- **純靜態多檔案網站**：`index.html` + `style.css` + `script.js`，圖片放在專案根目錄。可直接部署 GitHub Pages。
- **絕對不要使用 React／Vue／Next.js 等框架，不要 npm install、不要 build 步驟、不要打包工具。**
- **CSS、JS、圖片一律用相對路徑 `./`（例如 `./style.css`、`./photo.jpg`）。**
- 所有內容寫在單一頁面，用錨點連結切換段落。手機版需正常顯示。

## 部署到 GitHub Pages（做完就上線）

- 本專案已建立 repo：`https://github.com/oberonlai/oberonlai.github.io`，線上網址 `https://oberonlai.github.io/`。
- 每次改完網站檔案，**自動執行** `git add -A && git commit -m "更新網站" && git push`，不用問要不要 push。
- Pages 約 30–60 秒後更新，重整瀏覽器即可看到最新版。

## 不要起本機伺服器、不要自己做預覽檔

- 預覽一律用 GitHub Pages 的網址，不要起任何本機伺服器，也不要做臨時合併檔。
- 不要在瀏覽器打開本機的 `index.html`（`file://`）來預覽。唯一的預覽方式是部署後開 GitHub Pages 網址。

## 速度優先：一次生成就部署

- 一次把網站生成完就部署，不要做截圖打磨迴圈。
- 上線後要不要再美化，等使用者看完提出再針對他說的地方改（改完一樣自動 push）。

## 完工前品質檢查

- [ ] `index.html`／`style.css`／`script.js` 與圖片都在，沒有框架、沒有 build 產物。
- [ ] CSS／JS／圖片全部用相對路徑 `./`。
- [ ] hero 主標題一行講清楚「我幫誰解決什麼」。
- [ ] 內容用的是 website-brief.md 的真實文字，沒有 lorem ipsum 或假資料。
- [ ] 手機版不爆版，圖片都有 alt。

## 素材位置（做網站前一定要先讀專案資料夾）

- `photo.jpg`：個人形象照，用 `./photo.jpg` 引用並補上 alt。
- `CleanShot 2026-08-18 at 17.31.18.jpg`：履歷截圖，所有經歷、技能、數字、推薦人皆以此為準。
- `website-brief.md`：訪談整理出的需求主檔。
- 找不到的資訊寧可標「（待補）」或詢問，不要自己編造經歷、數字或客戶案例。
